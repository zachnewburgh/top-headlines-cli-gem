require 'spec_helper'

describe TopHeadlines do
  it 'has a version number' do
    expect(TopHeadlines::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
